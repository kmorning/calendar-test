source 'https://rubygems.org'

# specify ruby version
ruby '2.2.0'
# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.5'
# Use postgresql as the database for Active Record
gem 'pg'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# See https://github.com/rails/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Adds Foundation CSS and Javascript files to the asset pipeline
gem 'foundation-rails', '5.4.5.0'

# Use simple form with Foundation
gem 'simple_form'

# Simple Calendar
#gem "simple_calendar", "~> 2.0"

# Try to fix foundation scripts turbolinks break.
gem 'jquery-turbolinks'

# momentjs needed by fullcalendar
gem 'momentjs-rails'

# Try FullCalendar instead
gem 'fullcalendar-rails'

# For repeating event schedules
gem 'ice_cube'

# For simpler active record queries
gem 'squeel'

# A helper for creating declarative interfaces in controllers
gem 'decent_exposure'

# Haml
gem "haml"

# Date picker - from git for rails 4 support
#gem 'jquery_datepicker', git: 'https://github.com/foodforarabbit/jquery_datepicker.git'

# User jquery-ui instead for datepicker
gem 'jquery-ui-rails'

# timepicker
gem 'jquery-timepicker-rails'

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use Unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'
end

group :development do
  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Gem to rename rails app
  gem 'rename'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'

  # rails_layout gem to generate files for application layout, navigation links, and flash messages styled with Foundation CSS classes and layout.
  gem 'rails_layout'
end

